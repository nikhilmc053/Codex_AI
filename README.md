# Junk or No Junk Food List

A simple Next.js app that displays a homepage with common food items and labels each one as either **Junk** or **No Junk**.

## Getting started

Install dependencies and start the development server:

```bash
npm install
npm run dev
```

Then open http://localhost:3000 in your browser.
